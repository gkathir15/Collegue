<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msf.collegue">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_reunion"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_reunion"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".FCMTokenId">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <!-- <service -->
        <!-- android:name=".FirebaseMessageService" -->


        <!-- android:enabled="true" -->
        <!-- android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.MESSAGING_EVENT"/> -->
        <!-- </intent-filter> -->
        <!-- </service> -->
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home_actiity"></activity>
    </application>

</manifest>